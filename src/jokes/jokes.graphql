type Joke {
  id: ID!
  joke: String!
  likes: Int
  dislikes: Int
  user: User
}

type Query {
  allJokes: [Joke!]!
}

type Mutation {
  jokes: JokesMutations
}

type JokesMutations {
  createJoke(joke: String!): JokeResponse!
}

type JokeResponse {
  recordId: ID
  record: Joke
  error: String
}
